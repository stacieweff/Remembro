'use strict';

describe('Service: todoSvc', function () {

  // load the service's module
  beforeEach(module('remembroApp'));

  // instantiate service
  var todoSvc;
  beforeEach(inject(function (_todoSvc_) {
    todoSvc = _todoSvc_;
  }));

  it('should do something', function () {
      
  });

});
